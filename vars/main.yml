#SPDX-License-Identifier: MIT-0
---
# vars file for harbor

# Internal constants – DO NOT OVERRIDE

harbor_systemd_service_name: harbor

# Docker binary path (can be overridden if needed)
harbor_docker_bin: /usr/bin/docker

# Docker compose command
harbor_compose_bin: "{{ harbor_docker_bin }} compose"